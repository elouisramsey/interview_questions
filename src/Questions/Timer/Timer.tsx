import TimerInputs from './TimerInputs'

type Props = {}

// create a timer component that accepts two inputs for minutes and seconds and counts down the timer

const Timer = (props: Props) => {
 

  return (
    <div>
      <TimerInputs />
    </div>
  )
}

export default Timer
